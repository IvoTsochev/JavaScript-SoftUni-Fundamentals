function solve(arr1, params) {
    let elementsToTake = params[0];
    let elementToRemove = params[1];
    console.log(elementToRemove);
    
    let newArr = arr1.slice(0, elementsToTake);

    
}

solve(  [5, 2, 3, 4, 1, 6],
        [5, 2, 3]
     );